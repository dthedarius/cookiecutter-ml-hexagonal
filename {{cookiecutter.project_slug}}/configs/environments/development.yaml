app:
  debug: true
  log_level: DEBUG
  host: "0.0.0.0"
  port: 8000

model:
  threshold: {{ cookiecutter.prediction_threshold }}

mlflow:
  tracking_uri: "http://localhost:5000"
  experiment_name: "{{ cookiecutter.project_slug }}_dev"
